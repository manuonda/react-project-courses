import React from 'react';
import styles from './FirstButton.module.css'

const FirstButton = () => {
    return (
        <div>
            <button className={styles.button}>FirstButton</button>
        </div>
    );
};

export default FirstButton;