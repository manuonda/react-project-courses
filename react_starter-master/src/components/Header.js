import React from 'react';
import PropTypes from 'prop-types';

const Header = props => {
    return (
        <div>
           <h1>Radon Users Application</h1>    
        </div>
    );
};

Header.propTypes = {
    
};

export default Header;